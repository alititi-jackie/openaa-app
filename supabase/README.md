# Supabase

This project uses a new Supabase project only. Do not connect to or reuse the old `openaa-ny` Supabase project.

Phase 2 adds SQL migrations, seed structure, RLS policies, and docs only. It does not run migrations against any hosted database from this workspace.
